</main>
</div>
<div
  class="relative hidden flex-1 items-center justify-center overflow-hidden bg-brand-300 md:flex dark:bg-neutral-900">
  <div class="pointer-events-none absolute inset-0 opacity-20"
    style="background-image: radial-gradient(circle at 20% 20%, white 1px, transparent 1px); background-size: 28px 28px;">
  </div>
  <div class="pointer-events-none absolute -right-24 -top-24 h-72 w-72 rounded-full bg-brand-400/40 blur-3xl"></div>
  <div
    class="pointer-events-none absolute -bottom-24 -left-16 h-72 w-72 rounded-full bg-brand-800/40 blur-3xl dark:bg-brand-500/10">
  </div>

  <div class="relative flex w-full max-w-sm flex-col items-center px-8 text-center">
    <div class="animate-float">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" fill="none" class="h-32 w-32 drop-shadow-lg">
        <rect x="10" y="34" width="100" height="72" rx="14" fill="white" fill-opacity="0.12" stroke="white"
          stroke-opacity="0.5" stroke-width="2" stroke-dasharray="6 6" />
        <circle cx="60" cy="52" r="20" fill="white" fill-opacity="0.95" />
        <path d="M60 42v20M52 54l8-8 8 8" stroke="var(--color-brand-600)" stroke-width="3.2" stroke-linecap="round"
          stroke-linejoin="round" />
        <rect x="30" y="80" width="60" height="8" rx="4" fill="white" fill-opacity="0.55" />
        <rect x="38" y="93" width="44" height="6" rx="3" fill="white" fill-opacity="0.35" />
      </svg>
    </div>
    <h2 class="mt-8 text-xl font-semibold tracking-tight text-slate-800 dark:text-neutral-300">Drag, drop, done.</h2>
    <p class="mt-2 text-sm leading-relaxed text-slate-800 dark:text-neutral-300">
      Pull any file straight from your desktop into the browser. We'll handle the rest.
    </p>
    <div class="mt-6 flex items-center gap-2 text-xs text-slate-800 dark:text-neutral-300">
      <span class="h-1.5 w-1.5 rounded-full bg-emerald-400 dark:bg-emerald-300"></span>
      Encrypted in transit
    </div>
  </div>
</div>
</div>

<script>
  const themeToggle = document.getElementById('theme-toggle');
  if (themeToggle) {
    themeToggle.addEventListener('click', () => {
      const isDark = document.documentElement.classList.toggle('dark');
      localStorage.setItem('reckon-theme', isDark ? 'dark' : 'light');
    });
  }
</script>
</body>

</html>